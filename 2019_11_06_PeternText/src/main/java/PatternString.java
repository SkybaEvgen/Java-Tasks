public class PatternString {
    public static boolean textPattern (String pattern, String text) {
        char[] arrPattern = pattern.toCharArray();
        char[] arrText = text.toCharArray();


        for (int i = 0; i < arrPattern.length; i++) {

        }

        if(arrPattern[arrPattern.length]==arrText[arrText.length]) {
            
        }

        return true;
    }



}
